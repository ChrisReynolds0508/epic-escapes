<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fund My Project!</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body class="bg-gray-100 text-gray-900">
  <div class="flex flex-col min-h-screen">
    <header class="bg-white shadow-md">
      <div class="container mx-auto flex justify-between items-center p-4">
        <h1 class="text-2xl font-bold">
          <span role="img" aria-label="plane">✈️</span>
          <a href="/" class="text-blue-500 hover:text-blue-700">Epic Escape</a>
        </h1>
        <nav>
          <!-- Conditionally render login or logout links -->
          {{#if logged_in}}
          <button id="logout" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Logout</button>
          <a href="/review" class="ml-4 text-blue-500 hover:text-blue-700">Write a Review</a>
          {{else}}
          <a href="/login" class="text-blue-500 hover:text-blue-700">Login</a>
          <a href="/signup" class="ml-4 text-blue-500 hover:text-blue-700">Signup</a>
          {{/if}}
        </nav>
      </div>
    </header>
    <main class="container mx-auto flex-grow mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer class="bg-white shadow-md mt-5">
      <div class="container mx-auto p-4 text-center">
        <p class="text-gray-600">&copy; 2024 Epic Escape</p>
      </div>
    </footer>
  </div>

  <script src="/js/logout.js"></script>
</body>

</html>
